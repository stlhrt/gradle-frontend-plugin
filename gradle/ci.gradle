ext {
    isMaster = System.env.'TRAVIS_BRANCH' == "master"
    isCI = System.env.'CI'
}

task ci {
    dependsOn 'check', 'coveralls'
    description = 'Continuous Integration tasks'
    group = 'Build'
}